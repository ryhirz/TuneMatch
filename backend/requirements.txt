# ---------- 核心依赖（必装，零重依赖即可运行） ----------
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
openai>=1.30
python-multipart>=0.0.9
httpx>=0.27

# ---------- 测试 ----------
pytest>=8.0
pytest-asyncio>=0.23

# ---------- 可选重依赖（按需安装，未装自动降级 501） ----------
# librosa>=0.10        # 音频特征提取（MFCC/频谱/节奏/调性）
# numpy>=1.26          # librosa 依赖
# soundfile>=0.12      # 音频解码
# openai-whisper       # 语音转文本
# pyacoustid>=1.2      # 音频指纹识曲（需 ACOUSTID_API_KEY）
# audioread>=3.0
# chromadb>=0.5        # 向量数据库（相似歌曲召回）
# reportlab>=4.0       # PDF 歌单导出
# pillow>=10.0         # 分享海报生成
